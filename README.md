# MedPACT Website (Next.js 14)

A Visual Studio Code–ready marketing site starter for MedPACT.

## Run locally

1) Install Node.js 18+ (recommended)
2) From the project root:

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Pages
- `/` Home (conversion-first, disruptive + innovation-forward)
- `/category` Healthcare Intelligence Infrastructure™ (named category definition)
- `/manifesto` The Post‑PE Healthcare Era (manifesto)
- `/platform` Platform overview
- `/team` Team
- `/resources` Resources placeholder
- `/contact` Briefing request

## Brand tokens
Tailwind config includes:
- `medpact.blue`
- `medpact.green`
- `medpact.black`

## Replace placeholders
- Logo: `components/Nav.tsx` and `components/Footer.tsx` use a gradient mark placeholder.
- Contact form: swap with HubSpot/Salesforce embed.
- Resources: link PDFs + demo video embed.

## Compliance note
Site copy intentionally avoids guarantees. Outcomes vary by organization.
